# Where the Ollama daemon is listening.
# Use 127.0.0.1 rather than localhost — Node resolves localhost to IPv6 ::1 first.
OLLAMA_HOST=http://127.0.0.1:11434

# Ceiling for long operations (model pulls, long generations), in milliseconds.
OLLAMA_TIMEOUT_MS=600000
